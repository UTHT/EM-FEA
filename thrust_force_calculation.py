import math
import numpy as np
from scipy import integrate


'''
Variables:
N = number of turns
I = current
g = air gap
mu = absolute permeability (mu_r = mu/mu_0)






B1 = (3*np.sqrt(2)*N*I/(np.sqrt((np.pi*g/mu)**2)))